import { useEffect } from 'react';

import './index.scss';

interface IProps {}

const AppBar = (props: IProps) => {
  useEffect(() => {}, []);

  return (
    <p className="app-bar" />
  );
};

export default AppBar;
