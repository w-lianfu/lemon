import { useEffect } from 'react';

import './index.scss';

interface IProps {}

const AppStand = (props: IProps) => {
  useEffect(() => {}, []);

  return (
    <p className="app-stand" />
  );
};

export default AppStand;
