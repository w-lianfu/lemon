import { useEffect } from 'react';

interface IProps {}

const AppStand = (props: IProps) => {
  useEffect(() => {}, []);

  return (
    <section>app stand</section>
  );
};

export default AppStand;
