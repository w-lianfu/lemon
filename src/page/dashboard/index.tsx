import { useEffect } from 'react';

const Dashboard = () => {
  useEffect(() => {
    return () => {};
  }, []);

  return (
    <section>
      <p>~~~ Dashboard Page ~~~</p>
    </section>
  );
};

export default Dashboard;
