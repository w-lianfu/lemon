import { useEffect } from 'react';

interface IProps {}

const AppLock = (props: IProps) => {
  useEffect(() => {}, []);

  return (
    <section>app lock...</section>
  );
};

export default AppLock;
